const config = {
    app: {
        port: 3000,  // Cổng mặc định của ứng dụng
    },
    db: {
        uri: "mongodb://127.0.0.1:27017/QuanLyMuonSach",  // Chuỗi kết nối MongoDB mặc định
    }
};

module.exports = config;
